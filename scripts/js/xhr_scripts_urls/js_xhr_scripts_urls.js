/**
 * Java script file that contains the urls to all the public PHP Scripts used in
 * the project
 */

/**
 * Base storage for all the PHP Scripts
 */
var BASE_PHP_SCRIPTS_DIR = "scripts/php/";

/**
 * Base storage for all the PHP worker Scripts
 */
var WORKER_SCRIPTS_DIR = BASE_PHP_SCRIPTS_DIR + "worker/";

/**
 * URL for the worker script file
 */
var WORKER_SCRIPT_URL = WORKER_SCRIPTS_DIR + "worker.php";

/**
 * URL for the counties worker script file
 */
var COUNTIES_URL = WORKER_SCRIPTS_DIR + "worker_counties.php";

/**
 * URL for the districts worker script file
 */
var DISTRICTS_URL = WORKER_SCRIPTS_DIR + "worker_districts.php";

/**
 * URL for the land owners worker script file
 */
var LAND_OWNERS_URL = WORKER_SCRIPTS_DIR + "worker_landowners.php";

/**
 * URL for the title deeds worker script file
 */
var TITLE_DEEDS_URL = WORKER_SCRIPTS_DIR + "worker_titledeeds.php";

/**
 * URL for the wards worker script file
 */
var WARDS_URL = WORKER_SCRIPTS_DIR + "worker_wards.php";
/**
 * URL for the title deed comments worker script file
 */
var TITLE_DEEDS_COMMENTS_URL = WORKER_SCRIPTS_DIR
		+ "worker_title_deeds_comments.php";
/**
 * URL for the title deed easements worker script file
 */
var TITLE_DEEDS_EASEMENTS_URL = WORKER_SCRIPTS_DIR
		+ "worker_title_deeds_easements.php";

/**
 * URL for the title deed nature worker script file
 */
var TITLE_DEEDS_NATURE_URL = WORKER_SCRIPTS_DIR
		+ "worker_title_deeds_natures.php";

/**
 * URL for the title deed nature worker script file
 */
var TITLE_DEEDS_PROPRIETORSHIP_URL = WORKER_SCRIPTS_DIR
		+ "worker_title_deeds_proprietorship.php";

/**
 * URL for the title deed nature worker script file
 */
var SYSTEM_USERS_URL = WORKER_SCRIPTS_DIR + "worker_system_users.php";

/**
 * URL for the title deed authentication worker script file
 */
var AUTHENTICATE_URL = WORKER_SCRIPTS_DIR + "worker_authenticator.php";
/**
 * URL for the title deed transactions worker script file
 */
var TITLE_DEED_TRANSACTIONS_URL = WORKER_SCRIPTS_DIR
		+ "worker_transactions.php";
/**
 * URL for the title deed transactions worker script file
 */
var PRINT_REPORTS_URL = WORKER_SCRIPTS_DIR
		+ "worker_reports.php";